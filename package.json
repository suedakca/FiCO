{
  "name": "fico-kasif",
  "version": "1.0.0",
  "description": "FiCo Kaşif RAG System",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install --prefix frontend && pip install -r backend/requirements.txt",
    "dev": "concurrently \"cd backend && python3 -m uvicorn main:app --reload --port 8000\" \"cd frontend && npm run dev\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "react-markdown": "^10.1.0"
  }
}
